# Valenbot v2.1 权限表

本文件描述当前实现；旧图的历史设计页不作为当前权限依据。完整类图和时序见 `java-spring-v2/权限与交互模型.md` 与 `outputs/uml-design/Valenbot_v2.1_类图与时序图.drawio`。

| 操作 | 管理员 | 技术人员 | 普通用户 |
|---|---|---|---|
| 查询主台账、详情、搜索、二维码、常规导出 | 全部有效记录及有效列 | 授权 SN 范围及有效列 | 仅绑定一台、仅客户可见有效列 |
| 新增主档、修改业务值 | 可以 | 可以；自动分配 SN 需专项授权 | 仅修改绑定机器人昵称 |
| 创建补充业务记录、编辑记录 | 可以 | 可以 | 禁止 |
| 删除业务记录、机器人 | 可以，逻辑删除 | 禁止 | 禁止 |
| 主台账列新增、编辑、归档、恢复 | 可以 | 禁止 | 禁止 |
| 补充业务表及字段定义 | 可以 | 禁止 | 禁止 |
| 用户、角色、启停、重置密码、绑定管理 | 可以 | 禁止 | 禁止 |
| SN 号段管理 | 可以 | 禁止 | 禁止 |
| SN 生成、作废、迁移、维修替换 | 可以 | 需 SN 专项授权 | 禁止 |
| 装拆、流程、维修、人工回读 | 可以 | 授权设备范围内可以 | 禁止 |
| 来源快照 | 可以 | 仅无范围限制账号 | 禁止 |
| 审计 | 可以 | 本人及授权机器人事件 | 禁止 |

权限在服务端执行，隐藏按钮不是权限边界。普通用户未绑定时展示“尚未绑定机器人，请联系管理员”，不能查询其他设备。客户名称存于主台账，权限使用稳定 UUID；修改名称或 SN 不改变客户归属。

重绑定、解除绑定对后续请求立即生效；角色、启停、密码变更使相关会话失效。管理员不能停用或降级自己，并始终保留至少一个有效管理员。密码使用 BCrypt，接口与审计不返回密码或散列。

新增列默认内部可见，管理员发布后客户仍只能读取绑定设备的该列。归档保留数据、配置和审计，SN 身份、客户绑定与昵称编辑白名单不会随展示列消失。技术人员的业务编辑权不包含字段定义及删除权。

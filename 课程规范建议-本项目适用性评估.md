# 课程规范建议对 Valenbot 的适用性评估

评估日期：2026-09-16。对象：工作区 `java-spring-v2`，v2.1.0。

依据：用户提供的《机器人SN管理项目-课程规范建议.md》及当前工程源码、迁移、测试、既有验收报告、独立发布目录的 GitHub Actions 配置。本次为静态核对，没有执行业务写入测试、修改程序或数据库，也没有上传本评估到 GitHub。

## 1. 总体判断

**值得采用，尤其适合作为下一轮质量改进清单。** 文档与本项目的技术栈及业务特点高度吻合，不是单纯套课程模板。最大的帮助是补齐业务规则和测试，再降低以后修改权限、装拆、维修等功能时的维护风险。

保留 Spring Boot + JDBC + PostgreSQL + Thymeleaf 的建议合理。现有动态列、JSONB、SN 迁移和历史追溯不适合为了使用 JPA 而整体重写。当前系统已有客户绑定、权限、审计及数据库迁移基础，应增量修正。

它不会直接带来新的业务模块，也不是视觉设计方案；不能据此认为任意 Excel 导入、自动设备烧录、运维服务或新 UI 已经实现。

## 2. 逐项核对与实际收益

下表的“确认”指源码确认；不等于本次已通过接口复现或对实际业务数据作出判断。

| 建议 | 当前工程核对 | 对项目的帮助 | 优先级 / 工作量 |
|---|---|---|---|
| 禁止新建时夹带质检、调试结果 | `RobotApi.create()` 使用通用字段校验；不像编辑接口那样拒绝 `qcResult`、`debugResult`。`Bootstrap` 定义了质检结果字段，`qcGuard()` 只检查该字段是否为“通过” | 避免台账显示已通过质检，却没有对应流程证据。属于业务正确性问题 | 最高 / 中：要同时明确历史导入例外及回归用例 |
| SN 规则输入校验 | `AdminApi.settings()` 直接把 `maxSequence` 转成 `Number`；字符串会类型转换失败，缺失值可能空指针，小数会被截断 | 将用户输错与服务器故障区分开，避免保存意外数值 | 高 / 小至中 |
| 新建默认状态 | `create()` 仅限制已传入的状态，没有自动补“在库”；数据库 JSON 默认值为空对象 | 台账、筛选和后续流程更一致 | 高 / 小；仅对新建生效，历史缺失不能自动改造成事实 |
| 流程日期校验 | `process()` 直接调用 `LocalDate.parse()`，缺失时使用当天。日期解析异常没有被现有参数错误处理器覆盖 | 减少错误日期引起的 500；区分实际业务日期和登记时间 | 高 / 小至中，补录规则先明确 |
| 严格且独立的测试 | CSV 单元测试只断言整个结果的开头；当前 HTTP 验收依赖专用夹具及前序状态 | 防止测试成功被误解为所有输入、权限和并发场景均正确 | 高 / 中 |
| 分离 HTTP、业务、SQL 职责 | API 直接执行 SQL，`Store` 职责集中；`LabelApi` 调用其他 API 类 | 后续新增功能可复用规则，更容易定位和单独测试 | 中 / 大，宜按业务逐步做 |
| 事务放到完整业务用例 | 当前 API 已有事务；`BindingService.update()`、`Store.allocate()` 要求调用者提供事务 | 新增后台任务等调用入口时不容易漏掉事务，保证业务与审计一起提交 | 中 / 中，须验证回滚及锁顺序 |
| 固定请求 DTO + 动态字段 Map | 当前广泛使用 Map、Object 和类型转换 | 接口含义更清楚、输入错误更早发现；动态字段仍可扩展 | 中 / 中；不能一次替换后忽略 PATCH 语义 |
| 权限过滤下推 SQL、分页及批量查询 | 列表先查全部机器人，再按权限与字段处理；账号、字段、模块存在重复查询 | 机器人和历史记录增长后，响应与内存占用更可控 | 中 / 中至大；未做性能测量，不能断言当前已慢 |
| 格式化及前端按功能拆分 | Java 和前端存在超长行；阅读与评审成本较高 | 对课程学习、团队交接及定位错误帮助直接 | 中 / 小至中，格式与业务修改应分开提交 |
| JPA 课程实践 | POM 使用 JDBC，没有 JPA | 有学习价值，对当前生产功能收益较低 | 低 / 另建练习，暂不替换现有持久化 |

工作量为相对估计，不是工期承诺；需在确定接口兼容范围与测试夹具后再排期。

## 3. 文档需要校正或补充的地方

### 3.1 外部副本与当前工作区不同

文档审阅路径是 `D:\JAVAproject\Valenbot-v2.1.0\java-spring-v2`，当前真实工程位于 `C:\3_work\SN码管理系统\java-spring-v2`。文档中的本地源码链接不能直接作为本机链接。

文档提到的 `tests/repro_smoke.py` 在当前工程中不存在。因此，不能说本工程已有一个“同时接受 400 和 500”的测试，也不能把另一副本的复现记录当成本次运行结果。其测试方法上的批评合理；本工程可独立确认的问题是 CSV 测试断言不完整，以及现有 HTTP 验收未覆盖上述全部输入路径。

### 3.2 要精确区分错误类型

- `Store.integer()` 使用 `Integer.parseInt()`；常见非数字输入抛出的 `NumberFormatException` 已由 `IllegalArgumentException` 处理器转为 400，不能统称所有数字输入都会返回 500。
- `maxSequence` 的直接类型转换是另一个路径，仍有缺口。
- `LocalDate.parse()` 的 `DateTimeParseException` 属于日期时间异常，不属于 `IllegalArgumentException`；现有通用异常处理会将其作为 500。应定向处理，不能把所有异常一律改为 400。
- 若采用 DTO 的整数属性，还应确认 JSON 反序列化是否允许浮点转整数；仅添加 `@Min`、`@Max` 不一定能拒绝已被截断的小数。

### 3.3 不是所有建议都意味着当前故障

Controller 上声明事务当前可以生效，迁往 Service 是让业务入口更可靠，不等于现有事务全部失效。Java 中过滤权限存在扩展性成本，也不能据此断言已发生越权。JPA 缺席、使用原生 JavaScript、Thymeleaf 只生成页面外壳，都不是必须修复的问题。

文档建议查流程证据很有价值，但历史 Excel 导入可能只有部分日期、人员或报告信息。不能为了满足新规则生成虚假的“通过”记录；应明确新建、历史导入、后续质检的不同规则。

### 3.4 原有测试通过与新发现缺口可以同时成立

既有报告记录了 7 组 HTTP 验收及浏览器检查通过，覆盖客户隔离、账号管理、列管理、装拆和维修等特定场景。这不代表覆盖了所有创建参数和非法日期。

已发布的 GitHub Actions 当前只执行 `mvn clean verify`，工程中的 JUnit 有 2 项。它没有运行 Python 的 7 组数据库业务验收。因此，GitHub 绿色构建不能等同于全部业务验收通过。后续可增加独立 PostgreSQL 测试服务与合成数据，使核心业务验收能在持续集成环境自动复现，不上传真实工作簿和私有账号文件。

## 4. 对需求和界面的具体影响

对日常用户最直接的收益是：新机器人有确定状态、错误输入有明确提示、质检结果和流程记录一致、保存失败后不丢输入。分层和 DTO 主要让后续维护更稳定，通常不改变页面外观。

分页是需要同步调整页面的部分：明确每页数量、总数、搜索范围以及导出是否包含全部授权结果。权限必须先过滤再分页；搜索不能通过隐藏字段的命中情况泄露信息。

现有 Valenbot 工业工作台风格可以保留。这份文档不包含新的布局、配色或组件视觉方案，不建议把质量修复与大规模 UI 重做放在同一个版本中。

## 5. 建议如何落地

1. **先做一轮规则修复。** 使用独立 `robot_sn_v2_test` 和合成数据，先补会失败的用例，再修复新建流程字段、默认状态、数值和日期错误分类。拒绝非法请求时，同时断言配置、SN 占用、主档及审计没有部分写入。默认状态仅作用于新建记录。
2. **建立可信的回归基础。** CSV 每个危险前缀单独断言；把关键权限、并发装配、SN 分配、事务回滚和会话失效纳入可重复验收。保留原工作簿保真验证，但另提供不依赖真实数据的测试入口。
3. **以“新建机器人”为第一条分层链路。** Controller 接收请求，Service 负责权限、占号、建档、审计和事务，Repository 执行 SQL。接口保持兼容后再迁移其他业务，避免一次全面改写。
4. **按实际规模优化查询和页面。** 先记录查询次数和响应时间，再实施权限 SQL、批量加载和分页。账号及字段定义最多按请求复用；不能长期缓存在会话里导致重绑定后权限不立即更新。
5. **JPA 单独学习。** 保留 Flyway，不修改已发布 V1/V2。若以后确需数据库变更，新增迁移并验证升级及回退；不能用自动建表覆盖业务库。

规则修复可作为下一补丁版候选；分层与查询改造分开迭代。实施时保留现有 v2.1.0 发布包、旧工程及真实数据。应以“规则更可靠、改动更容易验证”为目标，而不是以新增类、注解或框架的数量衡量规范程度。

## 6. 本次核对入口

- [机器人创建、编辑、流程与列表](java-spring-v2/src/main/java/com/robotsn/RobotApi.java)
- [配置和账号管理](java-spring-v2/src/main/java/com/robotsn/AdminApi.java)
- [校验、权限、状态和编号规则](java-spring-v2/src/main/java/com/robotsn/Store.java)
- [异常响应](java-spring-v2/src/main/java/com/robotsn/ApiErrors.java)
- [导出与标签](java-spring-v2/src/main/java/com/robotsn/LabelApi.java)
- [现有 JUnit](java-spring-v2/src/test/java/com/robotsn/LabelTest.java)
- [既有验收范围与结果](java-spring-v2/变更与测试报告-v2.1.0.md)

本评估是下一步改进依据，不是已完成修复或重新验收的声明。

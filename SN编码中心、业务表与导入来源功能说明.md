# SN 编码中心、业务表与导入来源功能说明

适用版本：**Valenbot v2.1.0**。源码位于 `C:\3_work\SN码管理系统\java-spring-v2`。本文区分 v2.0 已有功能、本次新增能力以及未实现能力。

## 1. 先分清四个页面

| 页面 | 解决的问题 | 什么时候使用 |
|---|---|---|
| 机器人信息管理 | 一台机器人一份主档，管理版本、客户名称、装拆、流程和维修 | 查看或维护具体机器人；打印 SN 二维码 |
| SN 编码中心 | 管理编号的产生、占用、号段和退役，避免重号 | 建档前准备编号，给生产人员分配号段 |
| 业务表 | 建立额外的补充台账 | 需要独立记录工装、服务事项等补充业务 |
| 导入来源 | 回答“台账最初从哪里来、如何解释原表” | 核对原始单元格、字段映射和导入决策 |

新增机器人主台账列，使用 **机器人信息管理 → 列管理 / 字段管理**。这和“业务表 → 新增字段”是两套不同的字段配置。

## 2. SN 编码中心

### 编码格式

`LBR-YYMM-TYPE-SEQ`，例如 `LBR-2609-P-0011`：

- `LBR` 是固定前缀。
- `2609` 表示 2026 年 9 月，月份必须有效。
- `S` 样机、`P` 小批试产、`M` 量产、`R` 维修替换。
- `0011` 是该年月、该类型的流水号，范围 0001–9999。

不同年月或不同类型分别计数；同一组内递增，不回收已分配号码。并发生成由 PostgreSQL 事务和计数器锁保护，SN 注册表还执行唯一性约束。

### 号段与编号状态

管理员可以为指定年月、类型和负责人分配一段连续编号。当前单次生成或分配数量为 1–1000，不得超过规则中的流水上限。

| 状态 | 含义 | 后续用途 |
|---|---|---|
| 未使用 `UNUSED` | 已生成，尚未建立主档或用于迁移 | 建档、迁移，或有原因地作废 |
| 已使用 `USED` | 已被机器人主档占用 | 查看对应主档，不能重新建档 |
| 已作废 `VOID` | 未使用编号被主动停用 | 保留记录，不再分配 |
| 已退役 `RETIRED` | 原 SN 已迁移，或主档被归档删除 | 保留历史，不能复用 |

号段只是编号资源，不等于机器人已完成生产。原 Excel 中仅填写 SN 的 8 条记录也已经建立主档，因此属于“已使用”，而非“未使用”。

### 编号与机器人建档

可以先生成未使用编号，再在机器人信息管理中使用该编号建立主档；也可以在具有 SN 生成授权时于新建主档时自动生成。编号生成、占用和主档写入处在事务中，失败不会留下半个主档。

维修替换使用 R 类型新 SN，填写原因和审批凭证。迁移后机器人的稳定 `robot_id` 不变，旧 SN、历史二维码、装拆和维修记录仍可追溯，客户绑定也不会因为 SN 变化而失效。

### 权限

| 操作 | 管理员 | 技术人员 | 普通用户 |
|---|---|---|---|
| 查看编码中心 | 是 | 业务可见范围 | 否 |
| 配置规则、分配号段 | 是 | 否 | 否 |
| 生成、作废、迁移、维修替换 SN | 是 | 需要 SN 专项授权 | 否 |
| 新增机器人主档 | 是 | **v2.1 新增**；自动生成 SN 仍须专项授权，没有授权可使用预留编号 | 否 |
| 删除机器人业务记录 | 是，归档删除 | 否 | 否 |

**例子：** 管理员为 2609/P 分配 20 个编号给技术人员。技术人员按实际设备逐个建档；发现其中一个标签印错且尚未建档时，具备专项授权者填写原因作废该号码，不把它重新分给下一台设备。

**实现边界：** 编码、号段、状态、唯一性、生成/作废、迁移和二维码在 v2.0 已有。本次完善技术人员新增主档及绑定随稳定身份保留。没有任意自定义 SN 结构、回收退役号码、无限位数序号；设备 Flash 仍是任务与人工回读比对，不是自动硬件写入。

## 3. 业务表

### 补充台账与主台账的区别

业务表适合记录额外的信息集合。管理员新建表后，再定义字段，然后新增或编辑记录。它不是给机器人主档加列的替代入口，也不会自动变成机器人的版本字段、客户绑定或硬件装配关系。

例如建立“工装检查台账”，添加“工装编号”“检查说明”两个字段。技术人员可新增今天的检查记录并修改说明；它不会自动修改某台机器人的 SN 或质检结果。需要机器人质检追溯时，仍应在该机器人档案中“登记流程 → 质检”。

### 管理方式与权限

- 管理员：建立、归档删除业务表；新增、归档删除字段；新增、修改、归档删除记录；导出。
- 技术人员：查看、导出，**v2.1 新增记录权限**，可以修改已有记录；不能删除记录，也不能新增/删除字段或表。
- 普通用户：没有补充业务表访问权限，仅访问本人绑定机器人的客户可见字段。

业务表使用数据库中的表定义、字段定义和 JSONB 记录存储，是元数据驱动的逻辑业务表，不在网页中开放任意 SQL 或数据库物理表操作。补充表字段目前为文本值；数字、日期、布尔等主台账列类型是在“机器人信息管理 → 列管理”中配置。

### 删除与历史

删除是归档隐藏，不直接清除真实记录和值。删除字段后，当前业务记录页面和导出会过滤该字段；原始 JSONB 值仍在，审计保留原值、变化、操作者、时间和原因。删除表会隐藏其当前访问入口，关联记录保留。

机器人主台账 v2.1 的列管理同样采用归档方式，并增加恢复列入口。两者都保留历史，但主台账列还统一控制客户可见性、排序、必填和表单；补充业务表不自动获得这些主档能力。

**实现边界：** v2.0 已有补充表、字段、记录管理与审计；v2.1 放开技术人员新增业务记录。尚未实现任意关联模型设计、计算列、Excel 公式、跨表外键设计器、补充表的客户共享权限或任意 SQL 建表。

## 4. 导入来源

### 保存的内容

本项目初始化使用 `LB_人形陪伴机器人SN码管控表.xlsx`，保存：

1. 来源文件名、SHA-256、导入人员/系统标识和导入时间。
2. “SN编码规则”“需求说明”“机器人信息管理”三个原始工作表。
3. 每个非空单元格的坐标、行列号与提取值。
4. 46 列到机器人字段或模组位置的映射。
5. 导入数量与语义决策，例如空白保持为空、交付日期不当成激活日期、测试结果不当成独立质检结果。

来源快照用于核对，不会随着日常修改自动改写。比如主档后来更换了固件版本，来源快照仍保留最初 Excel 的版本。

### 如何使用

管理员进入“导入来源”，查看导入概要，展开工作表并按单元格核对。没有机器人范围限制的技术人员也可查看；受限技术人员无法读取全量来源快照；普通用户无权访问。可导出来源 JSON 快照。

**例子：** 有人质疑第一台机器人的激活日期为何为空。管理员查看来源映射会发现，原 Excel M 列实际上是“交付日期”；2026-09-15 已保存到交付日期，没有被误写成激活日期。

### 不是任意 Excel 导入器

**已实现：** v2.0 的固定模板初始化提取工具、一次性导入、原始单元格/映射/决策留存，以及来源查看和导出。v2.1 继续兼容这些原始数据和快照。

**未实现：** 网页上传任意 Excel、在线映射任意列、导入前交互预览、重复行合并、覆盖已有台账、批量纠错或任意 Excel 格式兼容。不能把“导入来源”页面描述为具有上述能力。

现有 `tools/prepare_import.py` 用于已确认模板的新库初始化。应用重启不会重放原表覆盖现有数据。追加或覆盖导入需要另行设计比对、冲突处理、权限和回滚流程。

## 5. 本次新增的绑定与主台账列管理

管理员在“用户与权限”中给普通用户选择一台机器人、填写客户名称并说明原因。实际权限依据稳定 `user_id` 与 `robot_id`，不依赖客户名称和可变 SN。当前采用一台机器人对应一个普通客户账号，冲突时要求先明确解绑，不静默覆盖。未绑定用户显示“尚未绑定机器人，请联系管理员”。

管理员在“机器人信息管理 → 列管理 / 字段管理”直接管理主台账列：名称、数据类型、排序、必填、客户可见、归档和恢复。新增默认内部可见。显式公开后，普通用户也只能读取自己绑定机器人的字段，唯一编辑字段仍为昵称。

历史空值不自动补造。新建主档需要填写当前必填列；修改历史主档时，未触及的旧空值可以保留，已填写的必填值不能清空。创建后的字段类型固定；需要不同类型时新增正确字段，避免歪曲历史值。归档 SN、客户或昵称展示列，不会删除底层身份、绑定、昵称权限规则或审计能力。
